# Public Fixtures

These fixtures come from public Toss Securities web endpoints and are safe to keep in git as long as they do not include user-specific data.

Refresh them with:

```bash
python3 tools/fetch_public_fixtures.py fixtures/responses/public
```

Do not mix authenticated captures into this directory.
